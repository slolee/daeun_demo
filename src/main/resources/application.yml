spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true


  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa

server:
  port: 8080